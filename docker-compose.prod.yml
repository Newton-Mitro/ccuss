version: '3.9'

services:
  # -------------------------------
  # Laravel PHP-FPM App
  # -------------------------------
  app:
    build:
      context: .
      dockerfile: docker/production/php/Dockerfile
    container_name: prod_union_banking_app
    restart: unless-stopped
    working_dir: /var/www/union_banking
    volumes:
      - storage_data:/var/www/union_banking/storage
      - cache_data:/var/www/union_banking/bootstrap/cache
    environment:
      APP_ENV: 'production'
      APP_DEBUG: 'false'
      DB_HOST: 'mysql'
      DB_PORT: '3306'
      DB_DATABASE: 'union_banking'
      DB_USERNAME: 'union_banking_user'
      DB_PASSWORD: '${DB_PASSWORD}'
      REDIS_HOST: 'redis'
    depends_on:
      - mysql
      - redis
    networks:
      - prod_union_banking_network

  # -------------------------------
  # Nginx Web Server
  # -------------------------------
  nginx:
    image: nginx:alpine
    container_name: prod_union_banking_nginx
    restart: unless-stopped
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./docker/production/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - storage_data:/var/www/union_banking/storage
      - cache_data:/var/www/union_banking/bootstrap/cache
      - ./public:/var/www/union_banking/public:ro
    depends_on:
      - app
    networks:
      - prod_union_banking_network

  # -------------------------------
  # MySQL Database
  # -------------------------------
  mysql:
    image: mysql:8.1
    container_name: prod_union_banking_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: '${MYSQL_ROOT_PASSWORD}'
      MYSQL_DATABASE: union_banking
      MYSQL_USER: union_banking_user
      MYSQL_PASSWORD: '${DB_PASSWORD}'
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - prod_union_banking_network

  # -------------------------------
  # Redis Cache
  # -------------------------------
  redis:
    image: redis:alpine
    container_name: prod_union_banking_redis
    restart: unless-stopped
    networks:
      - prod_union_banking_network

  # -------------------------------
  # Laravel Scheduler
  # -------------------------------
  scheduler:
    build:
      context: .
      dockerfile: docker/production/php/Dockerfile
    container_name: prod_union_banking_scheduler
    restart: unless-stopped
    working_dir: /var/www/union_banking
    volumes:
      - storage_data:/var/www/union_banking/storage
      - cache_data:/var/www/union_banking/bootstrap/cache
    command: php artisan schedule:work
    depends_on:
      - mysql
      - redis
    networks:
      - prod_union_banking_network

  # -------------------------------
  # Laravel Queue Worker
  # -------------------------------
  queue:
    build:
      context: .
      dockerfile: docker/production/php/Dockerfile
    container_name: prod_union_banking_queue
    restart: unless-stopped
    working_dir: /var/www/union_banking
    volumes:
      - storage_data:/var/www/union_banking/storage
      - cache_data:/var/www/union_banking/bootstrap/cache
    command: php artisan queue:work redis --sleep=3 --tries=3 --timeout=90
    depends_on:
      - mysql
      - redis
    networks:
      - prod_union_banking_network

networks:
  prod_union_banking_network:
    driver: bridge

volumes:
  db_data:
  storage_data:
  cache_data:
